# plot_whatsapp_ogg

A tool to create `time`, `spectrogram`, or `mel` waveforms from Whatsapp Audio messages.

You can get either videos or images:

```
python plot_whatsapp_ogg.py audio.ogg mel output.png
```

or videos:

```
python plot_whatsapp_ogg.py audio.ogg mel output.mp4 --video
```
